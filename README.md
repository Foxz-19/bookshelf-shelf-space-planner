# Picnic Blanket Size Planner

A cheerful, live single-page planner for choosing a picnic blanket by group size, sitting style, and gear.

## Run checks

```sh
npm run test:all
```

The UI test requires Python Playwright once:

```sh
pip install -r requirements-dev.txt
playwright install chromium
```

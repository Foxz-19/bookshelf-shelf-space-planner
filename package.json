{
  "name": "verdant-water",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": { "build": "tsc", "test": "npm run build && node --test test/*.test.mjs", "start": "npx serve ." },
  "devDependencies": { "typescript": "^5.7.3" }
}
